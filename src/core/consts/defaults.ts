import { LogLevel } from "./logLevels";


export const DEFAULT_LOGGER_LEVEL: LogLevel = LogLevel.LOG;
export const DEFAULT_LOGGER_FILE_SIZE: number = 1024;
export const OUTPUT_DESTINATION: 'stdout' | 'file' = 'file';